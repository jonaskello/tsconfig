{
  "name": "repro-tsconfig-paths",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && cp tsconfig.json ./dist",
    "start": "cd dist && node -r tsconfig-paths/register ./src/config/server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsconfig-paths": "^3.1.1"
  },
  "dependencies": {
    "@types/node": "^9.3.0"
  }
}
